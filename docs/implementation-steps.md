# 分步实施计划

## 总览

| 步骤 | 内容 | 依赖 | 预计复杂度 |
|------|------|------|------------|
| 1 | 创建 Xcode 项目骨架 | 无 | ⭐ |
| 2 | 系统监控数据采集层 | 步骤 1 | ⭐⭐⭐ |
| 3 | 任务栏基础显示 | 步骤 2 | ⭐⭐ |
| 4 | 弹出面板 (Popover) | 步骤 3 | ⭐⭐ |
| 5 | 桌面窗口模式 | 步骤 4 | ⭐⭐ |
| 6 | 液态玻璃 UI 美化 | 步骤 5 | ⭐⭐ |
| 7 | 应用图标与品牌 | 步骤 1 | ⭐ |
| 8 | 偏好设置 | 步骤 5 | ⭐⭐ |
| 9 | 历史图表 | 步骤 2 | ⭐⭐ |
| 10 | 打包与发布准备 | 全部 | ⭐ |

---

## 第 1 步：创建 Xcode 项目骨架

### 目标
在 Xcode 中创建项目，搭建基本目录结构。

### 具体任务
1. 使用 Xcode 26.5 创建新 macOS App 项目
   - Template: macOS → App
   - Interface: SwiftUI
   - Lifecycle: SwiftUI App
   - Language: Swift
2. 创建目录结构：
   ```
   FloatMonitor/
   ├── App/FloatMonitorApp.swift
   ├── MenuBar/MenuBarView.swift
   ├── Window/ContentView.swift
   ├── Services/SystemMonitorService.swift
   ├── Models/SystemStats.swift
   └── Utils/
   ```
3. 配置 Info.plist：
   - 可选 LSUIElement = YES（隐藏 Dock 图标）
4. 基础 App 入口代码

### 验证标准
- [ ] `swift build` 或 Xcode Build 通过
- [ ] 应用可启动，显示空白窗口
- [ ] 目录结构清晰，文件组织合理

---

## 第 2 步：系统监控数据采集层

### 目标
编写 SystemMonitorService 获取实时系统数据。

### 具体任务
1. 定义 `SystemStats` 数据模型
2. 实现 CPU 监控（`host_processor_info`）
3. 实现内存监控（`host_statistics64`）
4. 实现温度监控（IOKit SMC）
5. 实现网络监控（`sysctl`）
6. 实现 GPU 监控（IOKit `IOAccelerator`）
7. 使用 Timer + @Published 实现定时刷新
8. 打印日志确认数据采集正常

### 验证标准
- [ ] 控制台每秒输出一次完整系统数据
- [ ] CPU 数值合理（0-100%）
- [ ] 内存数值与活动监视器一致
- [ ] 温度、网络、GPU 数据采集无崩溃

---

## 第 3 步：任务栏基础显示

### 目标
在 Mac 菜单栏显示 CPU 和内存占用。

### 具体任务
1. 创建 NSStatusBar 状态项
2. 显示文本 "CPU X% MEM X%"
3. 定时刷新显示内容
4. 处理菜单栏空间有限的场景

### 验证标准
- [ ] 菜单栏可见 CPU/内存百分比
- [ ] 数值实时变化
- [ ] 应用启动即显示，无需手动操作

---

## 第 4 步：弹出面板 (Popover)

### 目标
点击菜单栏项弹出详细监控面板。

### 具体任务
1. 使用 NSPopover + NSHostingView 承载 SwiftUI 视图
2. 创建面板内容视图：CPU / 内存 / 网络 / 温度 / GPU 模块
3. 各模块的详细展示组件
4. 点击外部自动关闭
5. 初步应用玻璃效果背景

### 验证标准
- [ ] 点击菜单栏弹出面板
- [ ] 所有模块数据显示正确
- [ ] 点击面板外部关闭
- [ ] 数据每秒刷新

---

## 第 5 步：桌面窗口模式

### 目标
提供独立桌面窗口。

### 具体任务
1. 创建窗口模式入口
2. 设计窗口布局（侧边导航或顶部 Tab）
3. 复用面板中的视图组件
4. 窗口置顶选项
5. 窗口尺寸管理

### 验证标准
- [ ] 可打开独立窗口
- [ ] 窗口数据与面板同步
- [ ] 窗口可拖拽和调整大小

---

## 第 6 步：液态玻璃 UI 美化

### 目标
全面应用 macOS 26 设计语言。

### 具体任务
1. `.glassBackgroundEffect()` 全面应用
2. SF Symbols 6 图标点缀
3. 圆角、阴影、半透明层次微调
4. 深浅色模式适配
5. 动画优化

### 验证标准
- [ ] 视觉效果美观现代
- [ ] 液态玻璃层次感明显
- [ ] 深浅色切换正常

---

## 第 7 步：应用图标与品牌

### 目标
设计应用图标。

### 具体任务
1. 设计 1024×1024 App 图标
2. 设计菜单栏模板图标
3. 配置 Asset Catalog

### 验证标准
- [ ] Dock/启动台显示应用图标
- [ ] 菜单栏图标清晰

---

## 第 8 步：偏好设置

### 目标
添加设置窗口。

### 具体任务
1. 设置界面 UI
2. 各项设置的 @AppStorage 绑定
3. 刷新间隔 / 显示模式 / 温度单位 / 网络单位
4. 开机启动
5. 窗口置顶

### 验证标准
- [ ] 设置窗口可打开
- [ ] 修改设置即时生效
- [ ] 重启后设置保留

---

## 第 9 步：历史图表

### 目标
记录历史数据并绘制趋势图。

### 具体任务
1. 内存中缓存 60 分钟历史数据
2. Swift Charts 折线图组件
3. CPU / 内存 / 网络图表
4. 集成到面板和窗口中

### 验证标准
- [ ] 图表随时间积累数据
- [ ] 折线图清晰展示趋势
- [ ] 性能良好（数据缓存不泄漏）

---

## 第 10 步：打包与发布准备

### 目标
应用可独立运行和分发。

### 具体任务
1. 代码签名配置
2. Release Build
3. 性能测试
4. README 编写
5. DMG 打包

### 验证标准
- [ ] Release 版未安装 Xcode 也可运行
- [ ] 监控服务自身 CPU < 2%
- [ ] 内存占用 < 50MB
